{
  "name": "@mist-vue/components",
  "version": "0.0.44",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "build": "vite build --config vite.es.config.ts",
    "build:umd": "vite build --config vite.umd.config.ts",
    "build:style": "vite build --config vite.style.config.ts",
    "build:all": "pnpm run build && pnpm run build:umd && pnpm run build:style",
    "test": "vitest  --coverage --environment jsdom",
    "coverage": "vitest run --coverage"
  },
  "keywords": [],
  "author": "adekang",
  "license": "ISC",
  "dependencies": {
    "classnames": "^2.4.0",
    "@popperjs/core": "^2.11.8",
    "async-validator": "^4.2.5"
  }
}
